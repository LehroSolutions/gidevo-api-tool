# bun.lock generation

This environment could not reach `bun.sh` / the npm registry, so `bun.lock` was not generated here.

On your machine:

```bash
bun install
git add bun.lock package.json bunfig.toml
```

Prefer `bun install --frozen-lockfile` in CI once the lockfile exists.
