# GIDEVO API Tool documentation

Current public documentation starts at [Documentation Home](index.html) or [Start here](current/start-here.md).

Canonical prose lives in `docs/current/*.md`. The HTML and JSON counterparts, portal, map, and release manifest are generated with `pnpm docs:build` and verified with `pnpm docs:check`. Historical material remains outside `docs/current` and is not a current source of truth.
