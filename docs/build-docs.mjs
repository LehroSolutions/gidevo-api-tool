#!/usr/bin/env node
/**
 * GIDEVO API Tool docs builder note
 *
 * HTML counterparts in this package were generated in the Quantumide
 * ultra-link style (sidebar groups, breadcrumbs, TOC, related pages,
 * pager, searchable portal) using product design tokens.
 *
 * Canonical hub: docs/.docs/
 * Public guides: docs/*.md (+ nested folders) also get .html/.json twins.
 *
 * Re-run the packaging upgrade script or adapt this note into a full
 * local generator when you want a self-contained node rebuild.
 */
console.log('GIDEVO API Tool: docs HTML already generated under docs/ and docs/.docs/');
