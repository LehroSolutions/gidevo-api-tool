"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.initCommand = initCommand;
// SPDX-License-Identifier: Apache-2.0 OR LicenseRef-LEHRO-Solutions-Commercial
// Copyright (c) 2025 LEHRO Solutions
const fs = __importStar(require("fs"));
const path = __importStar(require("path"));
const spinner_1 = require("../utils/spinner");
const telemetry_1 = require("../../core/telemetry");
const ui_1 = require("../utils/ui");
const config_1 = require("../utils/config");
const VALID_TEMPLATES = ['openapi', 'graphql'];
async function initCommand(options) {
    // Merge with config file defaults
    const mergedOptions = (0, config_1.mergeWithConfig)(options, 'init');
    const { template = 'openapi', output = '.' } = mergedOptions;
    const targetDir = path.resolve(output);
    // Validate template
    if (!VALID_TEMPLATES.includes(template.toLowerCase())) {
        ui_1.ui.error('Template Recognition Failed', `Unknown template identifier: ${ui_1.ui.highlight(template)}`);
        ui_1.ui.info('Available Templates', `Select one of: ${VALID_TEMPLATES.join(', ')}`);
        process.exit(1);
    }
    ui_1.ui.showCompactBanner();
    ui_1.ui.sectionHeader('INITIALIZING NEURAL ARCHITECTURE');
    telemetry_1.telemetry.track('init_start', { template });
    if (fs.existsSync(targetDir) && fs.readdirSync(targetDir).length > 0) {
        ui_1.ui.error('Workspace Contaminated', `Target directory is not empty: ${ui_1.ui.filePath(targetDir)}`);
        ui_1.ui.info('Resolution', 'Select a pristine directory or create a new one to ensure structure integrity.');
        telemetry_1.telemetry.track('init_fail', { reason: 'directory_not_empty' });
        process.exit(1);
    }
    const spinner = await (0, spinner_1.createSpinner)(`Synthesizing ${template} structure...`);
    if (spinner.start)
        spinner.start();
    try {
        // Artificial delay for UX feel - "Weighty" operations feel more valuable
        // but typically we should avoid this. However, file I/O is instant here.
        // We will just proceed.
        // Create directory structure
        fs.mkdirSync(targetDir, { recursive: true });
        fs.mkdirSync(path.join(targetDir, 'specs'));
        fs.mkdirSync(path.join(targetDir, 'generated'));
        fs.mkdirSync(path.join(targetDir, 'tests'));
        // Create base files
        const packageJson = {
            name: path.basename(targetDir),
            version: '1.0.0',
            description: `Generated ${template.toUpperCase()} API project`,
            scripts: {
                generate: 'gidevo-api-tool generate -s specs/api.yaml -o generated',
                validate: 'gidevo-api-tool validate specs/api.yaml',
                'validate:strict': 'gidevo-api-tool validate specs/api.yaml --strict'
            }
        };
        fs.writeFileSync(path.join(targetDir, 'package.json'), JSON.stringify(packageJson, null, 2));
        // Create sample spec based on template
        if (template === 'openapi') {
            const spec = {
                openapi: '3.0.0',
                info: {
                    title: 'Core API',
                    version: '1.0.0',
                    description: 'Architecture generated by GIDEVO'
                },
                servers: [
                    { url: 'http://localhost:3000', description: 'Development Node' }
                ],
                paths: {
                    '/health': {
                        get: {
                            summary: 'System Integrity Check',
                            operationId: 'getHealth',
                            tags: ['System'],
                            responses: {
                                '200': {
                                    description: 'System Functional',
                                    content: {
                                        'application/json': {
                                            schema: {
                                                type: 'object',
                                                properties: {
                                                    status: { type: 'string', example: 'healthy' },
                                                    timestamp: { type: 'string', format: 'date-time' },
                                                    metrics: {
                                                        type: 'object',
                                                        properties: {
                                                            uptime: { type: 'number' },
                                                            load: { type: 'number' }
                                                        }
                                                    }
                                                }
                                            }
                                        }
                                    }
                                }
                            }
                        }
                    }
                }
            };
            fs.writeFileSync(path.join(targetDir, 'specs', 'api.yaml'), JSON.stringify(spec, null, 2));
        }
        else if (template === 'graphql') {
            const schema = `# Generated by GIDEVO API Tool
# Neural Schema Definition

type Query {
  """
  Ping systems
  """
  ping: Pong!
  
  """
  System status check
  """
  systemStatus: SystemHealth!
}

type Pong {
  message: String!
  latency: Int!
}

type SystemHealth {
  status: String!
  uptime: Float!
  version: String!
}

schema {
  query: Query
}
`.trim();
            fs.writeFileSync(path.join(targetDir, 'specs', 'schema.graphql'), schema);
        }
        // Create README
        const readme = `# ${path.basename(targetDir)}
    
> Generated via **GIDEVO API Tool** - Enterprise Architecture Engine

## ⚡ Quick Start

\`\`\`bash
# Synthesize SDK
npm run generate

# Verify Integrity
npm run validate
\`\`\`

## 📂 Architecture

\`\`\`
${path.basename(targetDir)}/
├── specs/          # Source of Truth (OpenAPI/GraphQL)
├── generated/      # Machine-Synthesized Code
├── tests/          # Integrity Verification
└── package.json    # Project Manifest
\`\`\`
`.trim();
        fs.writeFileSync(path.join(targetDir, 'README.md'), readme);
        // Stop spinner and display success
        if (spinner.stop)
            spinner.stop();
        ui_1.ui.success('Project Architecture Synthesized');
        ui_1.ui.table(['Artifact', 'Purpose'], [
            [ui_1.ui.filePath('specs/'), 'API Source of Truth'],
            [ui_1.ui.filePath('generated/'), 'Target for Synthesis'],
            [ui_1.ui.filePath('tests/'), 'Verification Suite'],
            [ui_1.ui.filePath('package.json'), 'dependency Manifest'],
        ]);
        ui_1.ui.nextSteps([
            `cd ${path.basename(targetDir)}`,
            'Define your schema in specs/',
            'Run synthesis: npm run generate',
        ]);
        telemetry_1.telemetry.track('init_success', { template });
    }
    catch (error) {
        if (spinner.stop)
            spinner.stop();
        ui_1.ui.error('Synthesis Aborted', error.message);
        telemetry_1.telemetry.captureException(error);
        process.exit(1);
    }
}
//# sourceMappingURL=init.js.map